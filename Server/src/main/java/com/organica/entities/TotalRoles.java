package com.organica.entities;

public enum TotalRoles {
    ADMIN,
    USER;

    // boolean isValidRole(String roleName) {
    //     if (roleName == null || roleName.isEmpty()) {
    //         return false;
    //     } else if (roleName.equals(TotalRoles.ADMIN.name()) || roleName.equals(TotalRoles.USER.name())) {
    //         return true;
    //     } else {
    //         return false;
    //     }
        
    // }

}
